name: Build valiDTr

on:
  pull_request:
    branches: ["main"]
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write   # needed to push to GHCR on main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: "1.23"

      - name: Build binary
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          go build -o dist/valiDTr ./cmd/valiDTr
          chmod +x dist/valiDTr

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: valiDTr-linux
          path: dist/valiDTr
          if-no-files-found: error

      # Docker image build (push only on main push)
      - name: Login to GHCR
        if: ${{ github.event_name == 'push' }}
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push Docker image (main only)
        if: ${{ github.event_name == 'push' }}
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/validtr:${{ github.sha }}
            ghcr.io/${{ github.repository_owner }}/validtr:latest
